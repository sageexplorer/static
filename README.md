 * This is a project to integrate jenkins build with AWS. A simple HTML file is uploaded to s3 when the build passes upon commit to master. 
 
  * This project uses Jenkins, Blue Ocean, and Github. 
  
